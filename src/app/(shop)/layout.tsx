// Layout compartido para todas las páginas de tienda.
// Placeholder para futuras barras de filtros globales.
export default function ShopLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
